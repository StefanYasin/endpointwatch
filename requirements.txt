# Core system & monitoring
psutil==5.9.8
feedparser==6.0.11
requests==2.31.0

# AI integrations
openai==1.40.3

# GUI (Tkinter is built into Python, no pip needed)
# Database (sqlite3 is built into Python, no pip needed)

# Optional audio alerts:
#   pip install endpointwatch[audio]  -> simpleaudio
#   (endpointwatch runs silently without it)

# Dev
flake8==7.1.1
